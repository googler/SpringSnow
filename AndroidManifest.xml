<?xml version="1.0" encoding="utf-8"?>
<manifest
        xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.erhu"
        android:versionCode="1"
        android:versionName="1.0">
    <application
            android:icon="@drawable/sonata"
            android:label="@string/app_name"
            android:name=".activity.SSApplication">

        <activity android:name=".activity.IndexActivity"
                  android:label="@string/app_name"
                  android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.MusicListActivity" android:screenOrientation="portrait"/>
        <activity android:name=".activity.AlbumListActivity" android:screenOrientation="portrait"/>
        <activity android:name=".activity.ArtistListActivity" android:screenOrientation="portrait"/>
        <activity android:name=".activity.PlayActivity" android:screenOrientation="portrait"/>

        <service android:name=".service.MusicService">
            <intent-filter>
                <action android:name="com.erhu.media.MUSIC_SERVICE"/>
            </intent-filter>
        </service>
    </application>
</manifest>
